import clsx from 'clsx';

const labelClassNames = clsx('flex items-start gap-2 text-start text-black font-medium nj-label-text');

const isRequiredClassNames = clsx('ps-2 -mb-0.5 mt-0.5 text-red-500');

export default {
	labelClassNames,
	isRequiredClassNames,
};
