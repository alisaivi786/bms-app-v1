# A new update in the props has been added:

1. preventOutsideClick (boolean): if true, the modal will not close when clicking outside the modal

# How to use it:

same as the previous with the new props instead of the old ones:

1. isCloseOnOutsideClick is replaced with preventOutsideClick
